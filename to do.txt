to do list for quiz

1.HTML for quiz, create quiz and results
-buttons, forms, textarea, divs etc


2.@views.route('/', methods=['GET', 'POST'])
    @login_required
    do this for each page

def quiz():
    if request.method == 'POST': 
        note = request.form.get('note')#Gets the quiz from the HTML 

        if len(note) < 1:
            flash('Note is too short!', category='error') 
        else:
            new_note = Note(data=note, user_id=current_user.id)  #providing the schema for the note 
            db.session.add(new_note) #adding the question to the database 
            db.session.commit()
            flash('Note added!', category='success')

    return render_template("home.html", user=current_user)


3. learn methods of POST and GET